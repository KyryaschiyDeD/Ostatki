<Page
    x:Class="Остатки.spizdiliRemains"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Остатки"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
		<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
			<Button Content="Спиздить" CornerRadius="15" Width="400" x:Name="createProduct" VerticalAlignment="Center" Margin="0,10,0,0" HorizontalAlignment="Center" Click="createProduct_Click"/>
			<Button Content="Получить ссылки" CornerRadius="15" Width="400" x:Name="getLinks" VerticalAlignment="Center" Margin="0,10,0,0" HorizontalAlignment="Center" Click="getLinks_Click"/>
			<Button Content="Отформатировать ссылки" CornerRadius="15" Width="400" x:Name="formatLinks" VerticalAlignment="Center" Margin="0,10,0,0" HorizontalAlignment="Center" Click="formatLinks_Click"/>
			<TextBlock x:Name="textBlock1" />
		</StackPanel>
		<TextBlock HorizontalAlignment="Left" x:Name="Result" Margin="517,190,0,0" Text="" TextWrapping="Wrap" VerticalAlignment="Top"/>
		
	</Grid>
</Page>
